import java.awt.Color.red

// Quando colocamos !! isso quer dizer que vamos assumir o erro
// Quando colocamos ? deixamos que o Kotlin resolva o erro

fun main(){
    var s: String? = null
    var abc = readln()

    println(s?.length)
  //  println(abc!!)
}